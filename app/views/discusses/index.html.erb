<div class="main-content">
  <section class="common-title">
    <span class="pull-left">讨论中心</span>
  </section>

  <!-- <div class="panel panel-default">
    <div class="panel-body">
      <div class="row serach-content">
        <div class="col-sm-1 text-right serach-name">
          <span>系别：</span>
        </div>
        <div class="col-sm-11">
          <ul class="nav nav-pills serach-course">
            <li param="" class="active"><a href="#">全部</a></li>
            <%# @academies.each_with_index do |academy, index| %>
              <li param="<%#= academy.id %>"><%#= link_to academy.name, "#" %></li>
            <%# end %>
          </ul>
        </div>
      </div>
    </div>
  </div> -->

  <ul class="nav nav-pills discuss-menu">
    <li class="<%= 'active' if params[:type].blank? || params[:type] == 'new' %>"><%= link_to("最新话题", discusses_path) %></li>
    <li class="<%= 'active' if params[:type] == 'hot' %>"><%= link_to("热门话题", discusses_path(type: "hot")) %></li>
    <li class="<%= 'active' if params[:type] == 'wait' %>"><%= link_to("等待回复", discusses_path(type: "wait")) %></li>
    <li class="navbar-right create-discuss">
      <a id="newCreateBtn" href="javascript:void(0);"><span class="glyphicon glyphicon-plus" aria-hidden="true"></span>新建话题</a>
    </li>
  </ul>

  <div id="topicList" class="container discuss-list">
    <%= render "topic", locals: {topics: @topics} %>
  </div>
</div>


<div id="createDiscuss" class="modal fade">
  <%= render "new_modal" %>
</div>

<script type="text/javascript">
  $(function(){
    $("#newCreateBtn").click(function(){
      $("#createDiscuss").modal();
    });
  });
</script>

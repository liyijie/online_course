<a href="javascript:void(0)" class="btn btn-primary by-btn-bg"><strong>教学团队</strong></a>
<br><br>
<div class="row">
   <div class="col-sm-12">
    <% teacher_courses.each_with_index do |teacher_course, index|%>
			<div class="col-sm-3 text-center"><%= image_tag(teacher_course.try(:teacher).try(:image).try(:avatar).try(:url, :t_220_220), class: "img-rounded")%>
			</div>
	  <% end %>
	</div>
</div>